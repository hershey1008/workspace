# FunRec 推荐系统实战项目 (复制版)

这是一个基于 `datawhalechina/fun-rec` (web_project 目录) 的完整电影推荐系统示例项目，涵盖了离线训练、在线服务和前端展示的全流程实现。该系统基于 MovieLens-1M 数据集构建。

## 核心特性
- **多路召回**：YoutubeDNN 向量召回 + I2I 相似度召回 + 偏好类目召回，通过 Snake Merge 融合。
- **精准排序**：基于 DeepFM 的点击率预测模型。
- **冷启动处理**：采用 UCB 探索与利用算法解决冷启动问题。
- **多样性重排**：连续打散类型/年代，避免推荐结果的信息茧房。
- **完整工程链路**：打通了从特征工程、模型训练、在线服务到前端交互的全生命周期。

## 系统架构
1. **前端**：支持首页推荐、电影详情、搜索、用户认证及个人中心等功能。
2. **后端服务**：
   - **离线流水线**：包括特征处理（召回/排序特征）、模型训练（YoutubeDNN、DeepFM），并推送到本地部署及 Redis 特征库。
   - **在线流水线**：检测冷启动，串联多路召回、精排、重排，并最终组装推荐结果。
3. **基础设施**：
   - PostgreSQL 存储核心业务数据
   - Redis 提供高速特征缓存
   - Elasticsearch 用于电影的全文索引和搜索展示

> 注：此为 FunRec 推荐系统实战环境的介绍文档。
